---
import Certs from "@icons/Certs.astro";
import Projects from "@icons/Projects.astro";
import Support from "@icons/Support.astro";

---

<content class="flex gap-8 md:gap-0 flex-col md:flex-row justify-around bg-concrete rounded-3xl md:rounded-full py-10 px-14 my-10 shadow-md">
    <div class="flex flex-col md:flex-row gap-4 justify-center">
        <div class="flex justify-center md:justify-start">
            <Certs />
        </div>
        <div class="text-center md:text-left">
            <p class="text-xl font-semibold">+10 años</p>
            <p class="text-primary font-semibold">Experiencia</p>
        </div>
    </div>
    <div class="hidden md:block border-l-2" />
    <div class="flex flex-col md:flex-row gap-4 justify-center">
        <div class="flex justify-center md:justify-start">
            <Projects />
        </div>
        <div class="text-center md:text-left">
            <p class="text-xl font-semibold">+30 proyectos</p>
            <p class="text-primary font-semibold">Completados</p>
        </div>
    </div>
    <div class="hidden md:block border-l-2" />
    <div class="flex flex-col md:flex-row gap-4 justify-center">
        <div class="flex justify-center md:justify-start">
            <Support />
        </div>
        <div class="text-center md:text-left">
            <p class="text-xl font-semibold">Soporte</p>
            <p class="text-primary font-semibold">Online 24/7</p>
        </div>
    </div>
</content>