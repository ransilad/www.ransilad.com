<div class='hero-bg'>
  <div class='bg-right'></div>
  <div class='bg-left'></div>
</div>

<style>
  .hero-bg {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    display: block;
    z-index: -1;
    opacity: 0.7;
    overflow: hidden;
    background: linear-gradient(-90deg, #272727 1px, transparent 1px), linear-gradient(#272727 1px, transparent 1px),
      linear-gradient(-90deg, #272727 1px, transparent 1px), linear-gradient(#272727 1px, transparent 1px),
      linear-gradient(transparent 6px, transparent 6px, transparent 156px, transparent 156px), linear-gradient(-90deg, #272727 1px, transparent 1px),
      linear-gradient(-90deg, transparent 6px, transparent 6px, transparent 156px, transparent 156px), linear-gradient(#272727 1px, transparent 1px),
      transparent;
    background-size:
      32px 32px,
      32px 32px,
      256px 256px,
      256px 256px,
      256px 256px,
      256px 256px,
      256px 256px,
      256px 256px;

    &::after {
      content: '';
      position: absolute;
      inset: 60% 0 0;
      background: linear-gradient(transparent, var(--bg-color));
    }
  }

  .bg-right {
    position: absolute;
    top: -50%;
    left: 50%;
    width: 150vh;
    height: 150vh;
    opacity: 0.3;
    background: radial-gradient(circle, #00ca5b9c 0%, transparent 70%);
    animation: 5s intro;
  }

  .bg-left {
    position: absolute;
    top: 0%;
    left: -50%;
    width: 150vh;
    height: 150vh;
    opacity: 0.3;
    background: radial-gradient(circle, #008b3f90 0%, transparent 70%);
    animation: 5s intro;
  }

  @keyframes intro {
    0% {
      scale: 1.4;
      opacity: 0;
    }

    80% {
      scale: 0.9;
    }
  }
</style>
